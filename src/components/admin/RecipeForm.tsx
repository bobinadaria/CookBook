/**
 * @deprecated Import from "@/components/admin/RecipeForm" (the folder) instead.
 * This re-export keeps existing page imports working without changes.
 */
export { default } from "./RecipeForm/index";
